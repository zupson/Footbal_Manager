<Window x:Class="WpfApp.Windows.InitialSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Windows"
        mc:Ignorable="d"
        Title="InitialSettingsWindow"  Height="300" Width="450" MinHeight="300" MinWidth="450" WindowStartupLocation="CenterScreen" >
    <Grid Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ComboBox Name="cbLanguage" Grid.Row="0" Grid.Column="1" 
                  Height="30" Margin="10" MinWidth="150" VerticalAlignment="Center" >
        </ComboBox>

        <ComboBox Name="cbGender" Grid.Row="1" Grid.Column="1" 
                  Height="30" Margin="10" MinWidth="150" VerticalAlignment="Center">
        </ComboBox>

        <ComboBox Name="cbResolution" Grid.Row="2" Grid.Column="1" 
                  Height="30" Margin="10" MinWidth="150" VerticalAlignment="Center">
        </ComboBox>


        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">Jezik</Label>
        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">Spol tima</Label>
        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">Rezolucija</Label>
        


        <Button Grid.Row="3" Grid.Column="0" Width="100" Height="30" 
                Margin="10,30,10,10" VerticalAlignment="Center" HorizontalAlignment="Center" 
                IsDefault="True"
                Click="BtnSave_Click">Spremi</Button>
        
        <Button Grid.Row="3" Grid.Column="1" Width="100" Height="30" 
                Margin="10,30,10,10" VerticalAlignment="Center" HorizontalAlignment="Center"
                IsCancel="True"
                Click="BtnCancle_Click">Odustani</Button>

    </Grid>
</Window>
